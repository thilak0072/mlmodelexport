[build]
  command = "pip install -r requirements .txt && python api.py"
  publish = "public"
  functions = "functions"
[[redirects]]
  from = "/api/*"
  to =  "/.netlify/functions/:splat"
  status = 200
  force = true